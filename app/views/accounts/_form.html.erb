<%= form_for @account, :html => { :class => "form-horizontal account" } do |f| %>

    <% if @account.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@account.errors.count, "error") %> prohibited this account from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @account.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :sid, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :sid, :class => 'form-control' %>
    </div>
    <%= error_span(@account[:sid]) %>
  </div>
  <div class="control-group">
    <%= f.label :token, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :token, :class => 'form-control' %>
    </div>
    <%= error_span(@account[:token]) %>
  </div>
  <div class="control-group">
    <%= f.label :phone_number, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :phone_number, :class => 'form-control' %>
    </div>
    <%= error_span(@account[:phone_number]) %>
  </div>
  <div class="control-group">
    <%= f.label :app_sid, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :app_sid, :class => 'form-control' %>
    </div>
    <%= error_span(@account[:app_sid]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            accounts_path, :class => 'btn btn-default' %>

<% end %>
